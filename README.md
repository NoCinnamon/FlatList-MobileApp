## FlatList Practice

- syntax:

```bash
<FlatList                              // FlatList component, self closing component  
  keyExtractor = { things used for the key}
  data={data you want to output}       // props
  renderItem={( value )} => (          // props
    <Text> {} </Text>
  )
/>
```

